{
  "name": "kanban-agent-mcp",
  "version": "0.1.0",
  "description": "MCP server (stdio) that attaches and operates file-based kanban work-item queues (work-items/{pending,in-progress,blocked,done})",
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/gak4u/kanban-agent.git",
    "directory": "mcp"
  },
  "type": "module",
  "private": true,
  "main": "server.js",
  "engines": {
    "node": ">=20"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.0.0"
  }
}
